import { DataTable } from "@/components/ui/data-table";
import { db } from "@/lib/db";

const page = async () => {
  return (
    <div className="flex justify-center items-center flex-col bg-white"></div>
  );
};

export default page;
